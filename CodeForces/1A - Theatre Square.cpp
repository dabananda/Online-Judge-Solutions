#include <iostream>
using namespace std;

int main()
{
    int x, y, a, countx = 0, county = 0, resultx, resulty;
    cin >> x >> y >> a;
    while (x--)
    {
        resultx = resultx;
    }
    cout << countx + county << endl;

    return 0;
}
